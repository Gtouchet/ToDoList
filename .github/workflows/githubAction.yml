name: GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  Explore-GitHub-Actions:
  
    runs-on: ubuntu-latest
    
    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: 1
    
    steps:
      - name: Tests
      - run: dotnet test
